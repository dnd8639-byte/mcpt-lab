numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10          # optional: faster Hawkes filter (falls back to a loop without it)
scikit-learn>=1.3    # only for the trendline meta-label example
ccxt>=4.0            # only for data/download_binanceus.py
certifi
